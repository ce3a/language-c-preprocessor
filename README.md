# Basic C Preprocessor support in Atom

Adds syntax highlighting to files containing C Preprocessor directives in Atom.
Based on the `language-c` package grammar, but without the C language specifics.
